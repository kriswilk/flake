{
  features.development_version = {
    homeManager = {
      programs.gh = {
        enable = true;
      };

      programs.git = {
        enable = true;
      };

      programs.lazygit = {
        enable = true;
      };
    };
  };
}
