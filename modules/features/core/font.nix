{
  features.core_font = {
    nixos = {
      fonts.fontconfig = {
        enable = true;
      };
    };

    homeManager = {
      fonts.fontconfig = {
        enable = true;
      };
    };
  };
}
