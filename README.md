# flake

NixOS host & user configuration based on flakes and home-manager.

## Usage

Boot from a NixOS live CD/USB and run:

```
curl -LO https://kriswilk.github.io/flake/install.sh
chmod +x install.sh

./install.sh
```
